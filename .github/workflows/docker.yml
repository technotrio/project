name: Run Docker Image

on:
  push:
    branches:
      - main

jobs:
  run_docker_image:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run Docker imagw
